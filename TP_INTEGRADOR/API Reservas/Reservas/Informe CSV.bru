meta {
  name: Informe CSV
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/reservas/informe?formato=csv
  body: none
  auth: bearer
}

params:query {
  formato: csv
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
