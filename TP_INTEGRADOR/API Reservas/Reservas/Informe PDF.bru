meta {
  name: Informe PDF
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/reservas/informe?formato=pdf
  body: none
  auth: bearer
}

params:query {
  formato: pdf
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
