meta {
  name: Listar Reservas
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/reservas
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
