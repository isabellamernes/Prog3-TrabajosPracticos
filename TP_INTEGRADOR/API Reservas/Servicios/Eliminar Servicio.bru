meta {
  name: Eliminar Servicio
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/servicios/11
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
