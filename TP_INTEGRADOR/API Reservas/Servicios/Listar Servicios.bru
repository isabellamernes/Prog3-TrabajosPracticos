meta {
  name: Listar Servicios
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/servicios
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
