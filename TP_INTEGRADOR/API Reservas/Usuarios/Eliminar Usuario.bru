meta {
  name: Eliminar Usuario
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/usuarios/10
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
